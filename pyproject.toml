[project]
name = "build-with-claude"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.71.0",
    "ipykernel>=7.0.1",
    "python-dotenv>=1.1.1",
]
