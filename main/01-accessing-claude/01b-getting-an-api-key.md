# 01b - 获取 API Key

为了能向 Claude 发送请求，一个 API Key 是必须的。这可以在 https://console.anthropic.com 登录你的 Anthropic 账户，然后生成一个。记住，每个 API Key 只会显示一次，请复制下来并妥善保管它。

对于在中国等不受 Anthropic 支持的地国家/地区的用户，可以使用 OpenRouter 等 LLM 聚合平台来间接访问 Claude 模型。对应的，你可能需要在后续的内容中调整 Base URL 等参数。这不是本教程的核心内容，此处不再赘述。
